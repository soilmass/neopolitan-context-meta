# Vision — `<project>` (anonymized example output)

> **Note**: this is an anonymized illustrative output of `vision-
> author`, edited from a real project to remove identifying detail.
> Use as a shape reference, not a template — your project's vision
> should be authored fresh against your project's authority docs.

---

## Vision statement

`<project>` exists to give independent `<creator-segment>` a
publishable portfolio that holds its own next to agency work — at
1/100th the cost and 1/10th the effort.

## Why we exist

`<creator-segment>` need to be visible. Their existing options:

1. **Cargo** / **Format** / **Squarespace**. Templated; thousand-
   strong precedent; everyone's portfolio looks the same.
2. **Custom-built portfolio** (Webflow OR React + Vercel). Custom
   look but expensive — typically `<currency-amount-range>` and
   `<duration>`.
3. **Self-built**. Time-bandwidth-prohibitive for `<creator-
   segment>` who are practicing their craft, not their tool.

`<project>` is a fourth path: portfolios that look custom-built
because every page is composed from agency-grade primitives, but
the user pays `<currency-low-amount>` and ships in `<short-duration>`.

## Who it serves

- Independent `<segment-1>` (~`<percentage>`% of users)
- Small `<segment-2>` shops (~`<percentage>`% of users)
- Recently-graduated `<segment-3>` building their first portfolio
  (~`<percentage>`% of users)

NOT serving (out of scope):
- Established agencies with in-house design + engineering teams
  (`<reason>`).
- `<adjacent-segment>` (`<reason>`).

## What it does

Takes a creator's existing work + bio + aesthetic preferences →
produces a publishable portfolio site at a custom domain in
`<short-duration>`. The site is:

- Custom on the surface (typography, motion, layout) — distinctive
  per user.
- Standard underneath (perf, a11y, SEO, analytics) — the platform's
  job.

## How we measure success

| KPI | 6-month target | 12-month target |
|---|---|---|
| Active sites | `<n-low>` | `<n-medium>` |
| Conversion (signup → published site) | `<percent-target>`% | `<percent-stretch>`% |
| Net Promoter Score | `<nps-target>` | `<nps-stretch>` |
| Monthly recurring revenue | `<dollar-amount>` | `<dollar-amount>` |

KPI authority: `kpis.md`. OST authority: `ost.md`.

## What we will NOT do

- Build a CMS. (We're not Webflow.)
- Build a hosting product. (We're not Vercel.)
- Build a generic site builder. (We're not Squarespace.)
- Serve enterprise customers as the primary segment.

## Why now

`<creator-segment>` reaches `<size-of-segment>` people in `<region>`;
indicators show `<growth-trend>`. Tooling for the segment has not
caught up — Cargo / Format / Squarespace are 10-15-year-old products
with templated aesthetics. Custom-built sites cost too much.
`<project>` is the missing layer.

## Authority

- Operator's pitch deck v3 — `internal://<project>/pitch-v3.pdf`
- `<segment-research-source>` — `<segment-research-url>`
- `<vendor-comparison-research>` — `<comparison-url>`

---

*Authored 2026-04-15 by vision-author v0.1.1. Reviewed 2026-04-22
by founding team. Sign-off: 2026-04-23.*
